#![feature(inherent_associated_types)]
#![feature(generic_const_exprs)]
#![allow(dead_code)]

mod custom_op;
mod stack_vm;

use std::{
    collections::{BTreeMap, HashMap},
    fmt::Debug,
};

fn main() {
    println!("Hello, world!");
}
